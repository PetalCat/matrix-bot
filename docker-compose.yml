services:
  bot:
    build: .
    restart: unless-stopped
    volumes:
      - ./config.docker.yaml:/app/config.yaml
      - ./data:/app/data
      # Mount .env if you have one, or comment out
      - ./.env:/app/.env
    environment:
      - RUST_LOG=info,plugin_ai=debug
